# @codedock/protocol-ts

TypeScript protocol types, schemas, and codecs for Codedock Tunnel clients and integrations.

This package mirrors the language-neutral definitions under the repository `protocol/` directory. Generated bindings should remain synchronized with the canonical protocol schema.

The current protocol version is `1`. It covers tunnel opening, closing, data, heartbeats, acknowledgements, and errors.
