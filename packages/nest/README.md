# @codedock/nest

NestJS integration for the Codedock Tunnel TypeScript SDK.

The package should provide modules and injectable providers while keeping tunnel authentication and lifecycle operations in `@codedock/sdk-ts`.
