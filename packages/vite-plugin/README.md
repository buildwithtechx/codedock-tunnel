# @codedock/vite-plugin

Vite development integration for exposing local applications through Codedock Tunnel.

The plugin should use `@codedock/sdk-ts` and provide an opt-in development tunnel without changing production builds.
